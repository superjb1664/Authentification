{% extends 'base.html.twig' %}

{% block title %}CommentaireAtelier{% endblock %}

{% block body %}
    <h1>CommentaireAtelier</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ commentaire_atelier.id }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ commentaire_atelier.titre }}</td>
            </tr>
            <tr>
                <th>Message</th>
                <td>{{ commentaire_atelier.message }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ commentaire_atelier.date ? commentaire_atelier.date|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('commentaire_atelier_index') }}">back to list</a>

    <a href="{{ path('commentaire_atelier_edit', {'id': commentaire_atelier.id}) }}">edit</a>

    {{ include('commentaire_atelier/_delete_form.html.twig') }}
{% endblock %}

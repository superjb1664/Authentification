<form method="post" action="{{ path('commentaire_atelier_delete', {'id': commentaire_atelier.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ commentaire_atelier.id) }}">
    <button class="btn">Delete</button>
</form>

{% extends 'base.html.twig' %}

{% block title %}Boisson{% endblock %}

{% block body %}
    <h1>Boisson</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ boisson.id }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ boisson.titre }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ boisson.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('boisson_index') }}">back to list</a>

    <a href="{{ path('boisson_edit', {'id': boisson.id}) }}">edit</a>

    {{ include('boisson/_delete_form.html.twig') }}
{% endblock %}

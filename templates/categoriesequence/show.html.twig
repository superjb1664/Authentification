{% extends 'base.html.twig' %}

{% block title %}Categorie de sequence{% endblock %}

{% block menu %}
    {% import 'macro/macroMenu.html.twig' as macroMenu %}
    {{ macroMenu.menu('Abdo en acier', 8) }}

{% endblock %}

{% block body %}
    <h1>Categorie de sequence</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ categoriesequence.id }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ categoriesequence.titre }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>{{ categoriesequence.image }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('categoriesequence_index') }}">Retour à la liste</a>

    <a href="{{ path('categoriesequence_edit', {'id': categoriesequence.id}) }}">Modifier</a>

    {{ include('categoriesequence/_delete_form.html.twig') }}
{% endblock %}
